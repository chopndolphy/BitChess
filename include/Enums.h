#pragma once

enum Outcome {
    WhiteWins = 0,
    BlackWins = 1,
    Tie = 2
};
enum Color {
    White = 0,
    Black = 1
};
enum ViewType {
    Console = 0,
    GUI2D = 1,
    GUI3D = 2
};
enum ErrorCode {
    MOV01 = 0,
};